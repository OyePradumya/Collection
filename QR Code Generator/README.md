A simple python program to convert texts to a qrcode!

Just run the file and you should be good to go

PS: You may need to install pyqrcode module as "pip install pyqrcode" if this program happens to not run.
